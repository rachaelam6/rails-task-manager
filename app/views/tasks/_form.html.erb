
<%# moved our duplicated code (the same form in both our new.html.erb and our edit.html.erb) in a partial:  %>

<%= form_with model: task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.submit %>
<% end %>
